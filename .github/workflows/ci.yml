name: CI

on:
  pull_request:
    branches:
      - main
  push:
    branches:
      - main

env:
  UV_LOCKED: true

jobs:
  ci:
    name: Lint and test
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version-file: .python-version

      - name: Install uv
        uses: astral-sh/setup-uv@v7
        with:
          enable-cache: true

      - name: Install the project
        run: uv sync

      - name: Lint
        run: uv run ruff check --output-format=github .

      - name: Format
        run: uv run ruff format --diff

      - name: Type checks
        run: uv run ty check src

      - name: Check docstrings in tests
        run: uv run interrogate tests

      - name: Check complexity
        run: uv run complexipy

      - name: Test
        run: uv run pytest --inline-snapshot=disable --junitxml=junit/test-results.xml

      - name: Upload pytest test results
        uses: actions/upload-artifact@v7
        with:
          name: pytest-results
          path: junit/test-results.xml
          retention-days: 7
        if: ${{ always() }}

  test:
    name: Test combinations of Python and Pydantic
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.12", "3.13", "3.14"]
        pydantic: ["1.10.14", "2.12.5"]

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v6
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install uv
        uses: astral-sh/setup-uv@v7
        with:
          enable-cache: true

      - name: Install the project
        run: uv sync

      - name: Install pydantic==${{ matrix.pydantic }}
        run: uv pip install "pydantic==${{ matrix.pydantic }}"

      - name: Test
        run: uv run pytest --no-cov --inline-snapshot=disable
